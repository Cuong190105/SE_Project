import 'package:flutter/material.dart';
class ForgotpasswordScreenPhone extends StatelessWidget {
  const ForgotpasswordScreenPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
    );
  }
}